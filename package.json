{
  "name": "chao-game-online",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter @chao-game-online/client build && bun run --filter @chao-game-online/server build",
    "lint": "eslint --fix --cache packages/**/src/**/*.{ts,tsx}",
    "format": "prettier --write --cache .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.29.0",
    "eslint": "^9.29.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.20",
    "globals": "^16.2.0",
    "prettier": "^3.6.2",
    "typescript-eslint": "^8.34.1"
  }
}
